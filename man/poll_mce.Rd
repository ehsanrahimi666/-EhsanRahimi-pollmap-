% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_v2.R
\name{poll_mce}
\alias{poll_mce}
\title{Multi-criteria evaluation by weighted linear combination}
\usage{
poll_mce(
  criteria,
  weights,
  directions = NULL,
  standardize = TRUE,
  constraints = NULL
)
}
\arguments{
\item{criteria}{A named list of single-layer \code{SpatRaster}s (or a multi-layer
\code{SpatRaster}).}

\item{weights}{Either a named numeric vector (normalised to sum to 1) or a
square pairwise-comparison matrix, in which case AHP priorities and the
consistency ratio are computed.}

\item{directions}{Optional named vector of \code{"benefit"} (higher is better,
default) or \code{"cost"} (higher is worse) per criterion.}

\item{standardize}{Logical; min-max standardise each criterion to [0, 1]
(default \code{TRUE}).}

\item{constraints}{Optional list of Boolean (\code{0}/\code{1}) \code{SpatRaster}s multiplied
into the result.}
}
\value{
A single-layer \code{SpatRaster} of the integrated suitability, with the
weights and (if applicable) consistency ratio attached as attributes.
}
\description{
Integrates several criterion layers into one suitability surface. Each
criterion is (optionally) standardised to [0, 1] and combined as a weighted
sum, \eqn{S(x)=\sum_k w_k x_k(x)}, with optional Boolean constraint masks. This
is the overlay device used by ESTIMAP and PollMap (Zulian et al. 2013; Rahimi
et al. 2021), following weighted-linear-combination practice (Malczewski 2000)
with weights optionally from the analytic hierarchy process (Saaty 1977).
}
\references{
Saaty, T.L. (1977) \emph{Journal of Mathematical Psychology}, 15,
234-281. Malczewski, J. (2000) \emph{Transactions in GIS}, 4, 5-22.
}
\seealso{
\code{\link[=poll_estimap]{poll_estimap()}}
}
