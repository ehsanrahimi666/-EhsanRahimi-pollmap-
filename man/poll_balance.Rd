% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demand.R
\name{poll_balance}
\alias{poll_balance}
\title{Match pollination supply and demand}
\usage{
poll_balance(
  supply,
  demand,
  method = c("difference", "ratio"),
  standardize = TRUE
)
}
\arguments{
\item{supply}{A supply \code{poll_map} or \code{SpatRaster} (single layer).}

\item{demand}{A demand \code{poll_map} or \code{SpatRaster} (single layer).}

\item{method}{\code{"difference"} (standardised supply minus demand, default) or
\code{"ratio"} (supply / demand).}

\item{standardize}{Logical; min-max standardise supply and demand before
comparison (default \code{TRUE}).}
}
\value{
A \code{poll_map} with a \code{balance} layer and a \code{deficit} layer (1 where
demand exceeds supply, else 0).
}
\description{
Couples a supply surface (from any land-use or SDM model) with a demand surface
to locate matches, mismatches and deficits (Schulp et al. 2014; Fernandes et
al. 2020). Both are placed on a common scale, then compared by difference or
ratio.
}
\references{
Fernandes, J. et al. (2020) \emph{Ecosystems and People}, 16, 212-229.
}
\seealso{
\code{\link[=poll_demand]{poll_demand()}}
}
