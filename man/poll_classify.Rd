% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extras.R
\name{poll_classify}
\alias{poll_classify}
\title{Classify a pollination surface into ordinal classes}
\usage{
poll_classify(
  x,
  n = 5,
  labels = NULL,
  style = c("quantile", "equal"),
  layer = NULL
)
}
\arguments{
\item{x}{A \code{poll_map} or \code{SpatRaster}.}

\item{n}{Number of classes (default 5).}

\item{labels}{Optional class labels (length \code{n}); defaults to
very low / low / moderate / high / very high for \code{n = 5}.}

\item{style}{\code{"quantile"} (equal-count, default) or \code{"equal"} (equal-interval)
breaks.}

\item{layer}{Optional layer name (defaults to the first).}
}
\value{
A list with the classified \code{map} (a categorical \code{SpatRaster}) and an
\code{area} data frame (cells and hectares per class).
}
\description{
Reclassifies a pollination layer into \code{n} ordinal classes and returns the
classified raster together with the area in each class (Rahimi et al. 2021,
Iran case study).
}
