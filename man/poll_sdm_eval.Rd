% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occurrence.R
\name{poll_sdm_eval}
\alias{poll_sdm_eval}
\title{Cross-validated evaluation of a species-distribution model}
\usage{
poll_sdm_eval(
  occ,
  predictors,
  method = "glm",
  k = 5,
  block = TRUE,
  coords = c("Longitude", "Latitude"),
  n_background = 1000,
  seed = NULL
)
}
\arguments{
\item{occ}{A data frame of occurrences.}

\item{predictors}{A multi-layer \code{SpatRaster} of environmental predictors.}

\item{method}{Algorithm(s) passed to \code{\link[=poll_sdm]{poll_sdm()}}.}

\item{k}{Number of folds.}

\item{block}{Logical; if \code{TRUE} (default) folds are contiguous spatial blocks
rather than random subsets.}

\item{coords}{Length-2 names of the coordinate columns.}

\item{n_background}{Number of background points sampled per fold.}

\item{seed}{Optional random seed.}
}
\value{
A list of class \code{poll_eval} with a per-fold data frame and the mean
AUC and TSS.
}
\description{
Assesses the predictive performance of the \code{\link[=poll_sdm]{poll_sdm()}} pathway by k-fold
cross-validation, reporting the area under the receiver-operating
characteristic curve (AUC) and the true skill statistic (TSS). Folds may be
assigned at random or as spatial blocks; spatial blocking gives a less
optimistic and generally more honest estimate when records are spatially
autocorrelated (Roberts et al. 2017).
}
\examples{
library(terra)
set.seed(1)
preds <- rast(nrows = 30, ncols = 30, nlyr = 2,
              xmin = 0, xmax = 300, ymin = 0, ymax = 300)
names(preds) <- c("v1", "v2")
values(preds) <- cbind(runif(900), runif(900))
xy  <- xyFromCell(preds, which(values(preds[["v1"]]) > 0.6))
occ <- data.frame(Longitude = xy[, 1], Latitude = xy[, 2])
ev <- poll_sdm_eval(occ, preds, k = 3, block = FALSE,
                    n_background = 200, seed = 1)
ev$mean_auc
}
\references{
Roberts, D.R. et al. (2017) Cross-validation strategies for data with
temporal, spatial, hierarchical, or phylogenetic structure. \emph{Ecography}, 40,
913-929.
Allouche, O., Tsoar, A. & Kadmon, R. (2006) Assessing the accuracy of species
distribution models: prevalence, kappa and the true skill statistic (TSS).
\emph{Journal of Applied Ecology}, 43, 1223-1232.
}
\seealso{
\code{\link[=poll_sdm]{poll_sdm()}}, \code{\link[=poll_thin]{poll_thin()}}
}
