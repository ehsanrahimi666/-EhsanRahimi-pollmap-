% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{poll_sensitivity}
\alias{poll_sensitivity}
\title{Monte-Carlo sensitivity and uncertainty mapping}
\usage{
poll_sensitivity(run, n = 50, layer = NULL, probs = c(0.05, 0.95))
}
\arguments{
\item{run}{A function of no arguments (or of the iteration index) returning a
\code{poll_map} or single-layer \code{SpatRaster} for one draw. Typically closes over a
sampler such as \code{\link[=beta_landcover]{beta_landcover()}}.}

\item{n}{Number of draws.}

\item{layer}{Optional layer name to summarise when \code{run} returns multiple
layers (defaults to the first).}

\item{probs}{Quantiles to report (default 5th and 95th percentiles).}
}
\value{
A \code{poll_map} with \code{mean}, \code{sd}, \code{cv} and quantile layers.
}
\description{
Runs a model repeatedly under sampled inputs and summarises the resulting
distribution per cell, addressing the uncertainty in expert-derived resource
scores highlighted by Rouabah et al. (2024).
}
\seealso{
\code{\link[=beta_landcover]{beta_landcover()}}
}
