% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdm.R
\name{poll_sdm}
\alias{poll_sdm}
\title{Fit a species-distribution model for a pollinator}
\usage{
poll_sdm(
  occ,
  predictors,
  method = "glm",
  coords = c("Longitude", "Latitude"),
  n_background = 1000,
  seed = NULL
)
}
\arguments{
\item{occ}{A data frame of occurrences with coordinate columns.}

\item{predictors}{A multi-layer \code{SpatRaster} of environmental predictors.}

\item{method}{Character vector of algorithms to fit (\code{"glm"}, \code{"gam"}, \code{"rf"}).}

\item{coords}{Length-2 names of the coordinate columns.}

\item{n_background}{Number of background points to sample.}

\item{seed}{Optional random seed.}
}
\value{
An object of class \code{poll_model}.
}
\description{
Fits one or more correlative models relating occurrence to environmental
predictors and returns them as an ensemble (Elith et al. 2011; Perennes et al.
2021). Presence records are complemented with randomly sampled background
points. The self-contained algorithms are \code{"glm"} (always available), \code{"gam"}
(needs \pkg{mgcv}) and \code{"rf"} (needs \pkg{randomForest}); requesting several
fits an ensemble. For a fuller workflow (spatial-block cross-validation,
additional algorithms, ensemble weighting) the \pkg{flexsdm} package can be
used to build the fits externally and passed through unchanged.
}
\examples{
library(terra)
preds <- rast(nrows = 30, ncols = 30, nlyr = 2,
              xmin = 0, xmax = 300, ymin = 0, ymax = 300)
names(preds) <- c("v1", "v2"); values(preds) <- cbind(runif(900), runif(900))
xy  <- xyFromCell(preds, which(values(preds[["v1"]]) > 0.6))
occ <- data.frame(Longitude = xy[, 1], Latitude = xy[, 2])
fit <- poll_sdm(occ, preds, method = "glm", n_background = 200)
fit
}
\references{
Perennes, M. et al. (2021) \emph{Ecological Modelling}, 444, 109484.
}
\seealso{
\code{\link[=poll_sdm_project]{poll_sdm_project()}}, \code{\link[=poll_sdm_supply]{poll_sdm_supply()}}
}
