% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extras.R
\name{poll_cpf}
\alias{poll_cpf}
\title{Central-place-foraging pollination model}
\usage{
poll_cpf(
  lulc,
  landcover,
  guild,
  r_max = 2 * guild$alpha,
  layers = c("supply", "abundance")
)
}
\arguments{
\item{lulc}{A land-cover \code{terra} \code{SpatRaster} of integer codes, in a
projected coordinate system with square cells (linear units, e.g. metres).}

\item{landcover}{A \code{\link{poll_landcover}} table mapping each code in
\code{lulc} to nesting and floral indices.}

\item{guild}{A \code{\link{poll_guild}} giving the foraging distance
\code{alpha}.}

\item{r_max}{Profitable foraging range (map units); defaults to twice
\code{guild$alpha}.}

\item{layers}{Which output layer(s) to return: any of \code{"supply"} and
\code{"abundance"} (both by default).}
}
\value{
A \code{poll_map} with \code{supply} and/or \code{abundance} layers.
}
\description{
A model in which foragers exploit resources only within a profitable range,
inspired by the foraging-theory visitation model of Olsson et al. (2015). The
exponential kernel of the Lonsdorf family is replaced by a linear net-gain
kernel, \eqn{w(d)=\max(0,\,1-d/r_{\max})}, so that visitation declines with
distance and stops beyond the profitable range \eqn{r_{\max}}. This is a
tractable approximation of central-place foraging rather than the full
marginal-value optimisation; the exact optimisation is planned for a future
release.
}
\references{
Olsson, O. et al. (2015) \emph{Ecological Modelling}, 316, 133-143.
\doi{10.1016/j.ecolmodel.2015.08.009}
}
\seealso{
\code{\link[=poll_lonsdorf]{poll_lonsdorf()}}, \code{\link[=poll_modified]{poll_modified()}}
}
