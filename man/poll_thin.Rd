% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occurrence.R
\name{poll_thin}
\alias{poll_thin}
\title{Spatially thin occurrence records}
\usage{
poll_thin(
  occ,
  coords = c("Longitude", "Latitude"),
  dist = 5,
  crs_from = "EPSG:4326",
  crs_to = "EPSG:2056",
  max_n = NULL,
  seed = NULL
)
}
\arguments{
\item{occ}{A data frame of occurrences.}

\item{coords}{Length-2 character vector naming the longitude and latitude
columns.}

\item{dist}{Minimum spacing in kilometres; one record is kept per \code{dist} by
\code{dist} grid cell.}

\item{crs_from}{Coordinate reference system of \code{occ} (default \code{"EPSG:4326"}).}

\item{crs_to}{Projected CRS in which thinning is carried out.}

\item{max_n}{Optional cap on the number of retained records; a random subset
is taken if more survive thinning.}

\item{seed}{Optional random seed, for reproducibility of the cap.}
}
\value{
A data frame with the same columns as \code{occ}, containing the retained
rows, with an attribute \code{thin} recording the input and output counts.
}
\description{
Reduces spatial clustering in occurrence data by retaining at most one record
per grid cell of a chosen size, optionally followed by a random cap.
Opportunistic records accumulate around towns, roads and popular recording
sites; fitting a distribution model to unthinned data allows that sampling
effort to be modelled as habitat preference, and the resulting spatial
autocorrelation inflates apparent predictive performance. Thinning is a
standard remedy (Boria et al. 2014).
}
\details{
Thinning is performed on a projected grid, so \code{crs} should be a projected
(metric) coordinate reference system appropriate to the study region.
}
\examples{
set.seed(1)
# 200 records clustered in one place plus 40 spread out
occ <- data.frame(
  Longitude = c(rnorm(200, 8.5, 0.02), runif(40, 6, 10)),
  Latitude  = c(rnorm(200, 47.4, 0.02), runif(40, 46, 47.7)))
thinned <- poll_thin(occ, dist = 5)
nrow(occ)
nrow(thinned)
}
\references{
Boria, R.A., Olson, L.E., Goodman, S.M. & Anderson, R.P. (2014)
Spatial filtering to reduce sampling bias can improve the performance of
ecological niche models. \emph{Ecological Modelling}, 275, 73-77.
}
\seealso{
\code{\link[=poll_sdm]{poll_sdm()}}, \code{\link[=poll_sdm_eval]{poll_sdm_eval()}}
}
