% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_v2.R
\name{poll_modified}
\alias{poll_modified}
\title{Quality-weighted, flight-range pollination model (PollMap)}
\usage{
poll_modified(
  lulc,
  landcover,
  guild,
  max_dist = guild$alpha,
  layers = c("supply", "abundance")
)
}
\arguments{
\item{lulc}{A land-cover \code{terra} \code{SpatRaster} of integer codes, in a
projected coordinate system with square cells (linear units, e.g. metres).}

\item{landcover}{A \code{\link{poll_landcover}} table mapping each code in
\code{lulc} to nesting and floral indices.}

\item{guild}{A \code{\link{poll_guild}} giving the foraging distance
\code{alpha}.}

\item{max_dist}{Flight-range truncation (map units); defaults to \code{guild$alpha}.}

\item{layers}{Which output layer(s) to return: any of \code{"supply"} and
\code{"abundance"} (both by default).}
}
\value{
A \code{poll_map} with \code{supply} and/or \code{abundance} layers.
}
\description{
The modified Lonsdorf model of Rahimi et al. (2021): as \code{\link[=poll_lonsdorf]{poll_lonsdorf()}}, but
(i) the foraging neighbourhood is truncated at the flight range \code{max_dist}
(default one foraging distance) and (ii) visitation to each destination is
weighted by its floral availability relative to its accessible neighbourhood,
\deqn{PSF_i=\mathrm{kernel}(G)\cdot \frac{F_i}{\mathrm{kernel}(F)},}
so that habitat edges adjacent to floral-rich fields receive the highest scores.
}
\references{
Rahimi, E. et al. (2021) PollMap: a software for crop pollination
mapping. \emph{Journal of Ecology and Environment}, 45, 22.
Olsson, O. et al. (2015) \emph{Ecological Modelling}, 316, 133-143.
}
\seealso{
\code{\link[=poll_lonsdorf]{poll_lonsdorf()}}, \code{\link[=poll_kennedy]{poll_kennedy()}}
}
